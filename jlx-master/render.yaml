services:
  - type: web
    name: jlx-backend
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: MONGO_URI
        sync: false
      - key: secret_key
        sync: false
